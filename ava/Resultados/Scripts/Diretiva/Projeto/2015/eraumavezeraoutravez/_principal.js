"use strict";

angular.module("resultado")
.controller('EraumavezPrincipal2015Ctrl', ["$stateParams", "$http", "$scope", "$timeout", "$filter", "$location", "$state", "$modal", "$rootScope", function ($stateParams, $http, $scope, $timeout, $filter, $location, $state, $modal, $rootScope) {
    var self = this;
    var mgr;

    $scope.$emit("loadClassMain", "box_resultados");
    
} ]);
